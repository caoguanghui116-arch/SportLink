<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mashang.notificationservice.mapper.NotificationMapper">

    <select id="selectByUserId" resultType="com.mashang.notificationservice.domain.vo.NotificationVo">
        SELECT
            notification_id,
            user_id,
            title,
            content,
            type,
            related_id,
            is_read,
            create_time
        FROM
            notification
        WHERE
            user_id = #{userId}
            AND del_flag = 0
        ORDER BY
            create_time DESC
    </select>

    <select id="countUnread" resultType="int">
        SELECT
            COUNT(*)
        FROM
            notification
        WHERE
            user_id = #{userId}
            AND is_read = 0
            AND del_flag = 0
    </select>

    <update id="updateReadStatus">
        UPDATE notification
        SET is_read = 1
        WHERE notification_id = #{notificationId}
    </update>

    <update id="updateAllReadStatus">
        UPDATE notification
        SET is_read = 1
        WHERE user_id = #{userId}
        AND is_read = 0
    </update>

</mapper>
