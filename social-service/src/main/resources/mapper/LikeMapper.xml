<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mashang.socialservice.mapper.LikeMapper">

    <delete id="deleteByTargetAndUser">
        DELETE FROM `like`
        WHERE target_type = #{targetType}
            AND target_id = #{targetId}
            AND user_id = #{userId}
    </delete>

    <select id="existsByTargetAndUser" resultType="int">
        SELECT COUNT(*)
        FROM `like`
        WHERE target_type = #{targetType}
            AND target_id = #{targetId}
            AND user_id = #{userId}
    </select>

</mapper>
