<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mashang.eventservice.mapper.ScheduleMapper">

    <select id="page" resultType="com.mashang.eventservice.domain.vo.ScheduleVo">
        SELECT
            it.item_id,
            it.item_name,
            it.item_type,
            sc.referee_id,
            sc.`group`,
            sc.game_time,
            ven.venue_name AS main_venue,
            ven.venue_id
        FROM
            event_item AS it
        LEFT JOIN
            `schedule` AS sc
        ON it.item_id = sc.item_id
        LEFT JOIN
            venue AS ven
        ON ven.venue_id = sc.venue_id
        ${ew.customSqlSegment}
    </select>
</mapper>