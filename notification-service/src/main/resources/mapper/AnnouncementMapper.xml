<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mashang.notificationservice.mapper.AnnouncementMapper">

    <select id="selectAll" resultType="com.mashang.notificationservice.domain.vo.AnnouncementVo">
        SELECT
            announcement_id,
            title,
            content,
            meeting_id,
            publisher_id,
            status,
            create_time
        FROM
            announcement
        WHERE
            del_flag = 0
        ORDER BY
            create_time DESC
    </select>

    <select id="selectByMeetingId" resultType="com.mashang.notificationservice.domain.vo.AnnouncementVo">
        SELECT
            announcement_id,
            title,
            content,
            meeting_id,
            publisher_id,
            status,
            create_time
        FROM
            announcement
        WHERE
            meeting_id = #{meetingId}
            AND del_flag = 0
        ORDER BY
            create_time DESC
    </select>

    <select id="selectById" resultType="com.mashang.notificationservice.domain.vo.AnnouncementVo">
        SELECT
            announcement_id,
            title,
            content,
            meeting_id,
            publisher_id,
            status,
            create_time
        FROM
            announcement
        WHERE
            announcement_id = #{announcementId}
            AND del_flag = 0
    </select>

</mapper>
